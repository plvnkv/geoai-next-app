name: geoai-app

services:
  app:
    container_name: app
    image: geoai-app:prod
    build:
      context: .
      dockerfile: Dockerfile.prod
    restart: unless-stopped
    ports:
      - "3000:3000"
